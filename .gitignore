.cpcache/
ii_v_circle.aux
ii_v_circle.log
ii_v_circle.pdf
resources/*
resources/ii_v_circle.aux
resources/ii_v_circle.log
resources/ii_v_circle.pdf
**/.clj-kondo/.cache/
**/.lsp/.cache/
.venv-tpg/
__pycache__/
out/
*.ppm
data/
data/nonstarter.db
*.aux
*.log
*.out
*.fdb_latexmk
*.fls
*.toc
.clj-kondo/

# Build and dependency output — never tracked (2026-08-15). These had been
# committed and were then deleted on disk, so 1152 phantom deletions sat in the
# working tree: gitignore does not apply to already-tracked files, so the
# deletion has to be committed for the rule below to take effect.
pod-eigs/target/
node_modules/
